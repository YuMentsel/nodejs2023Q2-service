export enum LogLevels {
  log = 'log',
  error = 'error',
  warn = 'warn',
  debug = 'debug',
  verbose = 'verbose',
}

export enum LogFiles {
  dirPath = 'logs',
  logFile = 'logs.log',
  errLogFile = 'errors.log',
}
